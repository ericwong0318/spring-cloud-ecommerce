receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

  prometheus:
    config:
      scrape_configs:
        - job_name: 'spring-boot-apps'
          scrape_interval: 30s
          metrics_path: '/actuator/prometheus'
          static_configs:
            - targets:
              - 'config-server:8888'
              - 'eureka-server:8761'
              - 'gateway:8080'
              - 'product:8081'
              - 'category:8082'
              - 'auth-server:9000'
              - 'order-service:8083'
              - 'inventory-service:8084'
              - 'notification-service:8085'
              - 'payment-service:8086'

processors:
  batch:
    send_batch_size: 1024
    timeout: 5s

  k8sattributes:
    extract:
      metadata:
        - "k8s.pod.name"
        - "k8s.namespace.name"
        - "k8s.deployment.name"
        - "k8s.container.name"
        - "k8s.node.name"
        - "k8s.pod.ip"
    passthrough: false

  resource:
    attributes:
      - action: upsert
        key: deployment.environment
        value: local
      - action: upsert
        key: service.namespace
        value: spring-cloud-microservices

exporters:
  otlphttp:
    endpoint: ${env:SPLUNK_INGEST_URL}
    headers:
      X-SF-Token: ${env:SPLUNK_ACCESS_TOKEN}
    compression: gzip
    tls:
      insecure: false

  otlphttp/metrics:
    endpoint: ${env:SPLUNK_INGEST_URL}
    headers:
      X-SF-Token: ${env:SPLUNK_ACCESS_TOKEN}
    compression: gzip
    tls:
      insecure: false

  prometheus:
    endpoint: 0.0.0.0:8889

  prometheusremotewrite:
    endpoint: ${env:SPLUNK_INGEST_URL}/v1/gateway/prometheus/remote-write
    headers:
      X-SF-Token: ${env:SPLUNK_ACCESS_TOKEN}
    timeout: 30s
    tls:
      insecure: false

  logging:
    verbosity: detailed

extensions:
  zpages:
    endpoint: 0.0.0.0:55679

service:
  extensions: [zpages]
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch, resource]
      exporters: [otlphttp, logging]
    metrics:
      receivers: [otlp, prometheus]
      processors: [batch, k8sattributes, resource]
      exporters: [otlphttp/metrics, prometheusremotewrite, prometheus, logging]
    logs:
      receivers: [otlp]
      processors: [batch, resource]
      exporters: [otlphttp, logging]
